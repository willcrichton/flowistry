import { suite, test } from "mocha";
import { focus_test_commands } from "./mock_data/focus";
import { expect_commands } from "./util/helpers";

suite("Focus tests", async () => {
  // TODO: get these tests working
  // test("focus_select", expect_commands(focus_test_commands));
});
